/**
 * @file People.jsx
 * @description People directory page.
 * Full implementation: mid pane = user directory with search; left nav = leading
 * local posters (ward to county); right nav = leading regional/national posters.
 * Stub until directory build sprint.
 */

import StubPage from '../components/Layout/StubPage.jsx'

export default function People() {
  return <StubPage title="People" />
}
