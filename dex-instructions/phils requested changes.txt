Lets put tool tips on the icons in locations. 
from 1-6th. My Home/Location Selector/My Vote/Profile/Help/Settings

The UKCP logo top left of the page near the logo graphic. Make it a hot link to the Site Home, i.e home. Put a tooltip 'SiteHome'

Make a holder page for each of the other functions in the site. Obviously we already have Locations selector. My Home/ Location Selector/My Vote/Profile/Help/Settings

The Home page has 5 white text green background labels to show the environment. Replace them with hotlinks to the subpages.    You will have to add the 6th

When the location selector tool is clicked the location page loads into the UI. It needs to open with the vanity banner open also and the initial nav bar with The nations showing and the crumbtrail with the UK icon in it. 
The vanity banner needs its variable label set to UK and placing in black letters in the centre h/v of the vanity banner.
When ever a user clicks any location the image will change relevant to that location(we will add this later when i work out how to content up the site) Either way image r not you need to make the vanity label the name of the clicked item. You need to manage Country/Region/County/Constitiuency/Ward as well as City/Town/Village/Hamlet In this way the site is now location aware. 

The navigation bar only displays down to county. After this the nav moves to mid panes. I want you to make the political nav all the way from UK to Ward show on the nav bar. Much as it did originally. 

The L/R Pane Mid section is now a places navigator and a constituency/ward navigator. I want them to show always. i.e when the location page is clicked, it will open up ready to navigate the countries with the map and all UK places and Cont/Wards showing in the l/r nav. The mid pane should be filtered to City/Town/Village/Constituency. The buttons for the filters need to be split so they are separate for place and Con/ward. So places aligned to left edge with left panel places navigator and political constructs aligned right edge with the political navigator.
As user clicks one any context, map or navigators or hierarchy main nav and crumb the entire page needs to reflect that change. 

The items in the left mid pane nav and the right pane nav need to set the page focus if they can. i.e if user clicks a hamlet, its nav path and crumb is built with places being added by name to the end of county with a type label and name. And c/w being added to the nav and crumb as normal
 
The crumb trail fails to stay tidy. i.e if I click more than 1 ward location it will continually build the crumb, not taking back the previous ward. this seems to happen for a number of the lower nav items c?/w/c?/T?/V/H. It needs to stay religiously clean to the last context clicked wherever it was clicked from

I want to be able to select any and all contexts on the Locations Selector page. So the ability to add a clicked item to a notional saved list for the user is required. We can use the l/r nav sections to add a checkbox to anything selected. The checkbox keep that in a cookie or something for now.

As a user selects a location of any form it should be added to dialog on the page that lets them quickly hot jump to the location.





  





